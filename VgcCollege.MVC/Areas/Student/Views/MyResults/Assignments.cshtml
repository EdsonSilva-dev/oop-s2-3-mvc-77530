@model IEnumerable<VgcCollege.MVC.Areas.Student.ViewModels.StudentAssignmentResultViewModel>

@{
    ViewData["Title"] = "My Assignment Results";
}

<h1>My Assignment Results</h1>

<div class="card shadow-sm border-0 mt-3">
    <div class="card-body">
        <table class="table table-striped align-middle mb-0">
            <thead>
                <tr>
                    <th>Course</th>
                    <th>Assignment</th>
                    <th>Score</th>
                    <th>Feedback</th>
                    <th>Graded At</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var result in Model)
                {
                    <tr>
                        <td>
                            <strong>@result.CourseName</strong><br />
                            <small class="text-muted">@result.CourseCode</small>
                        </td>
                        <td>@result.AssignmentTitle</td>
                        <td>
                            @{
                                var percentage = result.MaxScore == 0 ? 0 : Math.Round((result.Score / result.MaxScore) * 100, 2);
                            }

                            <div>@result.Score / @result.MaxScore</div>

                            @if (percentage >= 70)
                            {
                                <span class="badge bg-success">@percentage%</span>
                            }
                            else if (percentage >= 40)
                            {
                                <span class="badge bg-warning text-dark">@percentage%</span>
                            }
                            else
                            {
                                <span class="badge bg-danger">@percentage%</span>
                            }
                        </td>
                        <td>@result.Feedback</td>
                        <td>@result.GradedAt.ToLocalTime().ToString("dd/MM/yyyy HH:mm")</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>