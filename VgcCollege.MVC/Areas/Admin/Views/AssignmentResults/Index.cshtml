@model IEnumerable<VgcCollege.MVC.Areas.Admin.ViewModels.AssignmentResultIndexViewModel>
@using Microsoft.AspNetCore.Mvc.Rendering

@{
    ViewData["Title"] = "Assignment Results";
    var assignmentOptions = ViewBag.AssignmentOptions as List<SelectListItem> ?? new();
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1>Assignment Results</h1>
    <a asp-action="Create" class="btn btn-primary">Create Assignment Result</a>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">@TempData["SuccessMessage"]</div>
}

<div class="card shadow-sm border-0 mb-3">
    <div class="card-body">
        <form method="get" class="row g-3">
            <div class="col-md-6">
                <label class="form-label">Assignment</label>
                <select name="assignmentId" class="form-select">
                    <option value="">-- All Assignments --</option>
                    @foreach (var item in assignmentOptions)
                    {
                        if (ViewBag.AssignmentId != null && ViewBag.AssignmentId.ToString() == item.Value)
                        {
                            <option value="@item.Value" selected>@item.Text</option>
                        }
                        else
                        {
                            <option value="@item.Value">@item.Text</option>
                        }
                    }
                </select>
            </div>

            <div class="col-md-2 d-flex align-items-end">
                <button type="submit" class="btn btn-outline-primary w-100">Filter</button>
            </div>
        </form>
    </div>
</div>

<div class="card shadow-sm border-0">
    <div class="card-body">
        <table class="table table-striped align-middle mb-0">
            <thead>
                <tr>
                    <th>Assignment</th>
                    <th>Course</th>
                    <th>Student</th>
                    <th>Score</th>
                    <th>Graded At</th>
                    <th class="text-end">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var result in Model)
                {
                    <tr>
                        <td>@result.AssignmentTitle</td>
                        <td>@result.CourseName</td>
                        <td>
                            <strong>@result.StudentName</strong><br />
                            <small class="text-muted">@result.StudentNumber</small>
                        </td>
                        <td>@result.Score / @result.MaxScore</td>
                        <td>@result.GradedAt.ToLocalTime().ToString("dd/MM/yyyy HH:mm")</td>
                        <td class="text-end">
                            <a asp-action="Details" asp-route-id="@result.Id" class="btn btn-sm btn-outline-info">Details</a>
                            <a asp-action="Edit" asp-route-id="@result.Id" class="btn btn-sm btn-outline-warning">Edit</a>
                            <a asp-action="Delete" asp-route-id="@result.Id" class="btn btn-sm btn-outline-danger">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>